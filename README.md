# clock
An electron app which displays the current time in an analog clock format

# node packages
Requires electron  
Written in pure javascript

# To run (Unix)
make install  
./run.sh  

# To run (Windows)
win_install.bat  
win_run.bat  
